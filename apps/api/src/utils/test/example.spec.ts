// Example test file to verify Jest setup
// TODO: Replace with actual unit tests for your API

describe('API Test Setup', () => {
  it('should verify Jest is working', () => {
    expect(true).toBe(true);
  });

  it('should perform basic arithmetic', () => {
    expect(2 + 2).toBe(4);
  });
});
