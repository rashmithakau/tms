# ================================================================
# AZURE WEB APP - API ENVIRONMENT VARIABLES
# ================================================================
# Copy these to Azure Portal → API Web App → Configuration → Application Settings

# Node Environment
NODE_ENV=production
PORT=8080

# Database (MongoDB Atlas or Azure Cosmos DB)
# Get from: MongoDB Atlas → Clusters → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/tms?retryWrites=true&w=majority

# JWT Secrets (Generate strong secrets)
# PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# Or use: https://generate-secret.vercel.app/32
JWT_SECRET=your-super-secret-jwt-key-here-change-this
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here-change-this
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Session Secret
SESSION_SECRET=your-session-secret-here-change-this

# CORS - Your UI Web App URL
APP_ORIGIN=https://tms-ui-yourname.azurewebsites.net
ALLOWED_ORIGINS=https://tms-ui-yourname.azurewebsites.net

# Email Configuration (SMTP)
# Gmail: Use App Password from Google Account → Security → 2-Step Verification → App passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/tmp/uploads

# Logging
LOG_LEVEL=info
ENABLE_LOGGING=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ================================================================
# AZURE WEB APP - UI ENVIRONMENT VARIABLES
# ================================================================
# Copy these to Azure Portal → UI Web App → Configuration → Application Settings

# API URL - Your API Web App URL
VITE_API_URL=https://tms-api-yourname.azurewebsites.net

# UI Port (Azure uses 8080 by default)
PORT=8080

# Environment
NODE_ENV=production
