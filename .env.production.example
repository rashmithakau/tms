# ================================================================
# AZURE WEB APP - COMBINED (API + UI) ENVIRONMENT VARIABLES
# ================================================================
# Copy these to Azure Portal → Web App → Configuration → Application Settings

# Node Environment
NODE_ENV=production
PORT=8080

# Database (MongoDB Atlas or Azure Cosmos DB)
# Get from: MongoDB Atlas → Clusters → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/tms?retryWrites=true&w=majority

# JWT Secrets (Generate strong secrets)
# PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# Or use: https://generate-secret.vercel.app/32
JWT_SECRET=your-super-secret-jwt-key-here-change-this
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here-change-this
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Session Secret
SESSION_SECRET=your-session-secret-here-change-this

# CORS - Not needed since UI and API are on same origin
# APP_ORIGIN=same-origin
# ALLOWED_ORIGINS=*

# Email Configuration (SMTP)
# Gmail: Use App Password from Google Account → Security → 2-Step Verification → App passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/tmp/uploads

# Logging
LOG_LEVEL=info
ENABLE_LOGGING=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ================================================================
# NOTE: VITE_API_URL is NOT needed
# The UI uses relative path /api to call the backend
# Both UI and API run on the same domain and port
# ================================================================
