# Azure App Service Environment Variables Template
# Copy these to Azure Portal → Your Web App → Configuration → Application Settings

# Node Environment
NODE_ENV=production
PORT=8080

# Database (MongoDB Atlas or Azure Cosmos DB)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/tms?retryWrites=true&w=majority

# JWT Secrets (Generate strong secrets: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-here
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here

# Application Origin (Your Frontend URL)
APP_ORIGIN=https://tms-ui.azurestaticapps.net

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password

# Session Secret
SESSION_SECRET=your-session-secret-here

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=https://tms-ui.azurestaticapps.net,https://your-custom-domain.com

# DUmmy data
